package assigment1.tucn.cs.database.config;

public class JDBConnectionFactory {
	public JDBConnectionWrapper getConnectionWrapper(String schema) throws JDBConnectionException {
		return new JDBConnectionWrapper(schema);
	}
}
